import React from 'react';

const ViewUser = ({params}) => {
    const userId = params.userId
    console.log(userId);

    return (
        <div>
            
        </div>
    );
};

export default ViewUser;